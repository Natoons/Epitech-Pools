module.exports = {
    drawTriangle: function(limit)   {
        i = 0;
        msg = "";
        while(i != limit) {
            msg += "$"
            console.log(msg);
            i++;
        }
    }
}